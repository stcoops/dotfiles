"""default applications and mod key for qtile configuration."""

mod = "mod4"

group_names = [["1", "2", "3", "4", "5", "6"]]

browser = "brave"

terminal = "kitty"

editor = "code" # set to (terminal + " -e nvim") if you want to use nvim or a terminal based editor
notepad = "tjournal"
